import { TimeRelativePipe } from './time-relative.pipe';

describe('TimeRelativePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeRelativePipe();
    expect(pipe).toBeTruthy();
  });
});
