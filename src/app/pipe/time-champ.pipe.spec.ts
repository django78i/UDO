import { TimeChampPipe } from './time-champ.pipe';

describe('TimeChampPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeChampPipe();
    expect(pipe).toBeTruthy();
  });
});
